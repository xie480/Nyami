module.exports = {
  preset: 'react-native',
  // Transform all files, but ignore node_modules except for specific packages that need ES6 support
  // Transform all files, but ignore node_modules except for specific packages that need ES6 support
  transformIgnorePatterns: [],
};
